<div data-answer-id={{{ answer.id }}}>
  <p>{{{answer.body}}}
    <div data-errors-answer-id={{{answer.id}}}>
    </div>
    <!-- <? if ({{{answer.links.length}}} > 0) ?>
      <div class="answer-links">
        <p>Links:</p>
        <ul>
          - answer.links.each do |l|
            div data-link-id="#{ l.id }"
              - if l.is_a_gist?
                = render 'shared/gist', url: l.url
              - else
                li= link_to l.name, l.url, target: :_blank
              - if current_user&.owner_of?(answer)
                = link_to 'Delete link', link_path(l), method: :delete, remote: true

    - if answer.files.attached?
      - answer.files.each do |file|
        p data-file-id="#{file.id}"= link_to file.filename.to_s, url_for(file), target: :_blank
     -->
      <p>
        {{{links}}}
      </p>
      <p>
        {{{files}}}
      </p>
      <div class="rating">
        <div class='rating-result'>
          Rating: 0
        </div>
      <p>
        <a class="vote" data-type="json" data-remote="true" rel="nofollow" data-method="post" href="/answers/{{{answer.id}}}/vote_for">vote for</a>
      </p>
      <p>
        <a class="vote" data-type="json" data-remote="true" rel="nofollow" data-method="post" href="/answers/{{{answer.id}}}/vote_against">vote against</a>
      </p>
      <div class="rating-errors"></div>
    </div>
  </p>
</div>
